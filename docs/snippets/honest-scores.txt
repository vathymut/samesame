!!! warning "Scores from a fitted model must be out of sample"
    `samesame` only sees scores — not how they were made. If scores come from a fitted model, generate them out of sample with `cross_val_predict`, `oob_decision_function_`, or a held-out set. In-sample scores create false separation and invalidate the test.
