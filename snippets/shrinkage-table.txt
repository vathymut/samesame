| `shrinkage` | Effect |
|-------------|--------|
| `0.0` | Plain density ratio. Strongest correction, highest variance. |
| `0.5` | Default. Good balance — start here and check `ESS/n` before lowering. |
| `1.0` | Uniform weights. No correction. |

Lower = more aggressive; higher = more conservative.
